path: infra/init.go
update_behavior:
  type: skip
body: |-
  package dal

  import (
    "{{.Module}}/infra/mysql"
    "{{.Module}}/infra/redis"
  )

  func Init() {
    redis.Init()
    mysql.Init()
  }
