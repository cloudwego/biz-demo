apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: reviews
spec:
  hosts:
    - reviews
  http:
    - route:
      match:
        - headers:
            baggage:
              exact: "env=dev"
            - destination:
                host: reviews
                subset: v2